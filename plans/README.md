# Implementation Plans

Generated by the improve skill on 2026-07-11. Execute the plan below as one
coordinated migration. Read the full plan before starting, honor its STOP
conditions, and update the status when done.

## Execution Order And Status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Convert the flake to dendritic flake-parts modules | P1 | L | - | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale)

## Dependency Notes

- Plan 001 is self-contained. Its internal steps deliberately establish a compatibility bridge before switching the flake entry point, then remove that bridge before completion.
- Plan 001 was executed and approved in isolated worktree `/tmp/opencode/nix-dendritic-execute` at commits `8b97803` and `5ee1357`; merging remains the operator's decision.

## Findings Considered And Rejected

- Remove the existing NixOS and Home Manager `modules.*` enable options during the migration: rejected because it would combine a behavioral redesign with an already broad architecture migration. Revisit after output parity is established.
- Turn generated hardware configurations and package recipes into flake-parts modules: rejected because generated NixOS modules and `callPackage` recipes are clearer in their native module/function forms. They will live under underscore-prefixed paths excluded by `import-tree`.
- Export every internal lower-level module publicly through `flake.modules`: rejected because these modules are implementation details and a large public module namespace is not an existing requirement. Custom typed top-level options provide the deferred-module merging needed by the dendritic pattern without expanding the public API.
