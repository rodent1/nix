# Implementation Plans

Generated by the improve skill on 2026-07-11. Execute TODO plans in the order
below unless their dependency notes permit parallel work. Each executor must
read its full plan, honor its STOP conditions, and update its status when done.

## Execution Order And Status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Convert the flake to dendritic flake-parts modules | P1 | L | - | DONE |
| 002 | Make flake checks validate every pull request revision | P1 | S | - | TODO |
| 003 | Pin cache-building actions to immutable commits | P1 | S | - | TODO |
| 004 | Scope the WSL 1Password credential to explicit commands | P1 | S | - | TODO |
| 005 | Add evaluation checks for critical flake contracts | P1 | M | - | TODO |
| 006 | Apply the provisioned password after opnix materialization | P1 | M | 005 | TODO |
| 007 | Validate workflow and nvfetcher configuration before merge | P2 | S | 002, 003 | TODO |
| 008 | Document the supported operator workflow | P2 | S | 002, 005, 007 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale)

## Dependency Notes

- Plan 001 is self-contained. Its internal steps deliberately establish a compatibility bridge before switching the flake entry point, then remove that bridge before completion.
- Plan 001 was executed and approved in isolated worktree `/tmp/opencode/nix-dendritic-execute` at commits `8b97803` and `5ee1357`; merging remains the operator's decision.
- Plans 002, 003, 004, and 005 are independent and may be executed in parallel.
- Plan 006 depends on Plan 005 because the password behavior change must be protected by a failing-then-passing configuration contract.
- Plan 007 depends on Plan 002's unprivileged PR event and Plan 003's immutable-action convention before adding more CI execution paths.
- Plan 008 runs last so its commands and CI description reflect the final behavior from Plans 002, 005, and 007.

## Findings Considered And Rejected

- Remove the existing NixOS and Home Manager `modules.*` enable options during the migration: rejected because it would combine a behavioral redesign with an already broad architecture migration. Revisit after output parity is established.
- Turn generated hardware configurations and package recipes into flake-parts modules: rejected because generated NixOS modules and `callPackage` recipes are clearer in their native module/function forms. They will live under underscore-prefixed paths excluded by `import-tree`.
- Export every internal lower-level module publicly through `flake.modules`: rejected because these modules are implementation details and a large public module namespace is not an existing requirement. Custom typed top-level options provide the deferred-module merging needed by the dendritic pattern without expanding the public API.
- Selectively build hosts/packages based on changed paths: rejected because six outputs do not justify the risk of missing effects from shared auto-imported modules, overlays, or lock changes.
- Consolidate all matrix builds into fewer jobs: rejected because setup savings are unmeasured and the change would reduce failure isolation and useful parallelism.
- Expand beyond `x86_64-linux`: rejected because the repository explicitly scopes itself to that platform and contains no contrary product intent.
