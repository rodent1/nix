{
  internal.homeModules.gamer = _: {
    config = {
      modules = {
        desktop = {
          enable = true;
          plasma.enable = true;
        };
        kubernetes.enable = true;
      };
    };
  };
}
